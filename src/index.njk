{% extends 'layouts/slides.njk' %}

{% block slides %}
  {% for slide in collections.slides %}
    <section data-slide-slug="{{ slide.fileSlug }}" class="slide slide-{{ slide.data.theme or global.defaultTheme }}">
      <div class="slide-content flow">
        {{ slide.templateContent | safe }}
      </div>

      {% if global.showSlideNumber %}
        <div class="slide-number">
          <span class="visually-hidden">Slide n°</span>
          {{ slide.data.order }}
        </div>
      {% endif %}
    </section>
  {% endfor %}
  {% if global.notes %}
    <script src="scripts/slides.js"></script>
  {% endif %}
{% endblock %}